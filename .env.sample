# Port (optional, default 3000)
# PORT=3000

# Option A: File paths (absolute or relative to project)
# PRIVATE_KEY_PATH=./keys/private.pem
# PUBLIC_KEY_PATH=./keys/public.pem

# Option B: Direct PEM contents (keep the BEGIN/END lines)
# RSA_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----
# RSA_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

# Enable mock mode (no real crypto; for quick testing)
# MOCK_MODE=true
